const CANVAS_WIDTH = 500;
const CANVAS_HEIGHT = 500;
const FRAMERATE = 60;

function setup() { 
  createCanvas(CANVAS_WIDTH, CANVAS_HEIGHT);
  frameRate(FRAMERATE);
}

function draw() {
  background(0);
}